#include <iostream>
using namespace std;
int search(int arr[],int x){
    
       int m,l,f;
       l = 0;
       int n = sizeof(temp) / sizeof(temp[0]);
       cout<<n;
       m = l + (l-f)/2;
       return f;
    }
 int main(){
    
    int arr[] = { 2, 3, 4, 10, 40 };
    int x = 10;
    int n = sizeof(arr) / sizeof(arr[0]);
    cout<<n;
    search(arr,x);
    return 0;
 }